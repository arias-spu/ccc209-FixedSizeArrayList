These instructions are for compilation and execution in a
Unix/Linux environment that has installed
bin-utils, make, g++


The following commands compile and link the main program
and run it
$ make
$ ./array

The following commands compile and link the Unit Test
and run it
$ make unittest
$ ./unittest
